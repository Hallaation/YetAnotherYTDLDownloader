<Window
   x:Class="YetAnotherYTDLDownloader.MainWindow"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:classes="clr-namespace:YetAnotherYTDLDownloader.Classes"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
   xmlns:local="clr-namespace:YetAnotherYTDLDownloader"
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   Title="MainWindow"
   Width="800"
   Height="450"
   mc:Ignorable="d">

   <Window.DataContext>
      <local:MainWindowViewModel />
   </Window.DataContext>
   <Border
      Margin="10"
      BorderBrush="Black"
      BorderThickness="2">
      <Grid Margin="5">
         <Grid.RowDefinitions>
            <RowDefinition Height=".05*" />
            <RowDefinition Height="*" />
         </Grid.RowDefinitions>

         <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*" />
            <ColumnDefinition Width="*" />
         </Grid.ColumnDefinitions>

         <TextBox
            Name="URLInput"
            Grid.Column="0"
            Margin="0"
            Text="{Binding InputURL, Mode=TwoWay}" />
         <Button
            Grid.Column="1"
            Margin="5,0,0,0"
            Command="{Binding AnalyzeAndAdd}">
            <TextBlock
               VerticalAlignment="Center"
               FontSize="10"
               Text="Analyze" />
         </Button>

         <Border
            Grid.Row="1"
            Grid.ColumnSpan="2"
            Margin="0,5,0,0"
            BorderBrush="Black"
            BorderThickness="2">
            <Grid Grid.Row="1" Grid.ColumnSpan="2">
               <Grid.ColumnDefinitions>
                  <ColumnDefinition Width="2*" />
                  <ColumnDefinition Width="*" />
               </Grid.ColumnDefinitions>
               <Grid.RowDefinitions>
                  <RowDefinition Height="1*" />
                  <RowDefinition Height="*" />
                  <RowDefinition Height=".1*" />
               </Grid.RowDefinitions>

               <Grid Grid.Row="0">
                  <Grid.RowDefinitions>
                     <RowDefinition Height=".1*" />
                     <RowDefinition Height="*" />
                  </Grid.RowDefinitions>

                  <TextBox
                     Grid.Row="0"
                     Height="20"
                     Text="{Binding SelectedVideo.Title}" />
                  <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                     <TextBox Text="{Binding SelectedVideo.Description}" />
                  </ScrollViewer>
               </Grid>
               <!--<ListView ItemsSource="{Binding VideoDetailList, Mode=OneWay}" SelectedIndex="{Binding SelectedURLIndex, Mode=OneWayToSource}">
                  <ListView.ItemTemplate>
                     <DataTemplate DataType="classes:VideoDetails">
                        <StackPanel ToolTip="{Binding Title}">
                           <TextBlock Text="{Binding Title}" />
                        </StackPanel>
                     </DataTemplate>
                  </ListView.ItemTemplate>
               </ListView>-->

               <Grid Grid.Row="1">
                  <Grid.RowDefinitions>
                     <RowDefinition Height=".2*" />
                     <RowDefinition Height="*" />
                  </Grid.RowDefinitions>
                  <Rectangle Grid.Row="1" Fill="AliceBlue" />
                  <ComboBox Name="VideoFormats" ItemsSource="{Binding SelectedVideo.Formats}">
                     <ComboBox.ItemTemplate>
                        <DataTemplate DataType="classes:VideoFormatDetails">
                           <TextBlock Text="{Binding FormattedText}" />
                        </DataTemplate>
                     </ComboBox.ItemTemplate>
                  </ComboBox>

               </Grid>

               <!--  Thumbnail  -->
               <StackPanel Grid.Column="1" Margin="5">
                  <ContentControl Margin="0,0,0,5">
                     <!--<Rectangle Fill="Red" />-->
                     <Image Grid.Column="1" Source="{Binding SelectedVideo.Thumbnail}" />
                  </ContentControl>
                  <CheckBox>
                     <TextBlock Text="Embed thumbnail" />
                  </CheckBox>
               </StackPanel>

               <Rectangle
                  Grid.Row="2"
                  Grid.ColumnSpan="2"
                  Fill="LightBlue" />

               <TextBox
                  x:Name="OutputDIR"
                  Grid.Row="2"
                  Margin="2,0,2,1" />
               <StackPanel
                  Grid.Row="2"
                  Grid.Column="1"
                  Orientation="Horizontal">
                  <Button>
                     <TextBlock
                        VerticalAlignment="Center"
                        FontSize="10"
                        Text="Do A Thing" />
                  </Button>
               </StackPanel>

            </Grid>
         </Border>

      </Grid>

   </Border>
</Window>
